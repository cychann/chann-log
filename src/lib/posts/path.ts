import path from "path";

export const POSTS_PATH = path.join(process.cwd(), "/posts");
export const ARTICLES_PATH = path.join(POSTS_PATH, "articles");
export const LOGS_PATH = path.join(POSTS_PATH, "logs");
